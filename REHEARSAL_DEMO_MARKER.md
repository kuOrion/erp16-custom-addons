# Rehearsal demo marker

Harmless test file, safe to ignore -- added to demo the admin console's Review/Approve-and-deploy flow with the spinner + auto-scroll fixes. First marker commit.
Second marker update, for testing the fixed spinner + auto-scroll-to-bottom behavior in the admin console.
