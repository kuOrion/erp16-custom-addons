# Admin console dry-run marker

Harmless marker file, same convention as the earlier
`DEPLOY_MECHANISM_TEST.md` used to prove `deploy.sh`'s promote step.
Standing in for a real dev's "Send for Review" push so the admin
console has something genuine to pick up via its next fetch.

Safe to delete once the walkthrough is done.

Second round: simulating another dev laptop's push, so the admin
console's "Recent changes" and "Pending review" cards both have
something fresh to show.
